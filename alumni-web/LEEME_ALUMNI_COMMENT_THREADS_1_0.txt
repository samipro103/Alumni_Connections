ALUMNI COMMENT THREADS 1.0

La migración de Supabase YA fue aplicada.

Permite:
- responder comentarios
- responder respuestas
- continuar el hilo sucesivamente
- notificar al autor del comentario respondido
- conservar ancho móvil limitando solo la sangría visual

Ejecutar:

node .\aplicar_alumni_comment_threads_1_0.js
npm run build

Si pasa:

git add .
git commit -m "Add threaded comment replies"
git push origin main

RECORDATORIO QA — BORRAR TODA LA SIMULACIÓN:

node .\scripts\qa-demo\cleanup_alumni_demo.js --confirm=DELETE-ALUMNI-QA
