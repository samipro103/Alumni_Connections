ALUMNI STORIES 1.3.3B

El parche 1.3.3 anterior NO guardó cambios porque se detuvo antes del fs.writeFileSync.

Este parche reemplaza el matcher del botón + por uno robusto.

EJECUTAR DESDE alumni-web:

node .\aplicar_alumni_stories_1_3_3b_option_c_exact_creator.js
npm run build

Si el build sale bien:

git add .
git commit -m "Stories 1.3.3 - exact Option C creator"
git push origin main
