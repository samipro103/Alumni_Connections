ALUMNI PROFILE 1.1.1B — BUILD HOTFIX

CAUSA CONFIRMADA
El parche Profile 1.1.1 tomó por error el `return (` que estaba dentro de:

if (!profile) {

como si fuera el render principal.

Eso dejó:
- todo el perfil profesional dentro de `if (!profile)`;
- una llave estructural faltante;
- riesgo de usar `profile.*` cuando profile es null;
- error Vercel:
  Expected '}', got '<eof>'

ADEMÁS
Quedaron importados simultáneamente:
- profile-professional-exact-1-1-1.css
- profile-option-3-own.css

Eso podía producir estilos montados aunque compilara.

QUÉ CORRIGE 1.1.1B
- restaura el AlumniEmptyState dentro de `if (!profile)`;
- mueve el render profesional a su lugar correcto;
- elimina el import del CSS viejo;
- elimina profile-option-3-own.css si existe;
- conserva profile-professional-exact-1-1-1.css;
- valida la sintaxis TSX con TypeScript antes de escribir;
- crea un backup local antes de modificar.

EJECUTAR DESDE alumni-web:

node .\aplicar_alumni_profile_1_1_1b_build_hotfix.js
npm run build

SI BUILD PASA:

git add .
git commit -m "Profile 1.1.1B - fix profile build structure"
git push origin main
