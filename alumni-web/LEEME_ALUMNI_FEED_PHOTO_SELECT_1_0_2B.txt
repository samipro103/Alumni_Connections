ALUMNI FEED PHOTO SELECT 1.0.2B — ROBUST NULL STAGE HOTFIX

POR QUÉ EXISTE

El parche 1.0.2 corrigió correctamente:

src/components/feed/ImageCropEditor.tsx

pero se detuvo al llegar a:

payload/src/components/feed/ImageCropEditor.tsx

porque esa copia tiene una estructura diferente.

ESTE HOTFIX

- revisa src/
- revisa files/src/
- revisa payload/src/
- corrige únicamente donde exista el patrón nullable
- si una copia tiene otra estructura, la omite
- NO aborta por copias antiguas/diferentes

EJECUCIÓN

node .\aplicar_alumni_feed_photo_select_1_0_2b_null_stage_robust.js
npm run build

SI BUILD PASA

git add .
git commit -m "Feed Photo Select 1.0.2B - robust crop stage fix"
git push origin main

NO CAMBIA DISEÑO.
