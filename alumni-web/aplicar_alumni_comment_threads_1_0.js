const fs = require("fs");
const path = require("path");
const ROOT = process.cwd();
const MARKER = "ALUMNI_COMMENT_THREADS_1_0";
const F = {
  feed: "src/app/feed/page.tsx",
  sheet: "src/components/feed/FeedCommentsSheet.tsx",
  css: "src/app/feed/feed-comments-messaging-font.css",
  notifications: "src/app/notifications/page.tsx",
  migration: "supabase/migrations/20260911131000_alumni_comment_threads_1_0.sql",
};
function p(rel){ return path.join(ROOT, rel); }
function fail(m){ console.error("❌ " + m); process.exit(1); }
function read(rel){ if(!fs.existsSync(p(rel))) fail(`No encontré ${rel}. Ejecutá dentro de alumni-web.`); return fs.readFileSync(p(rel),"utf8").replace(/\r\n/g,"\n"); }
function backup(rel,content){ const b=p(rel)+".before-comment-threads-1.0.bak"; if(!fs.existsSync(b)) fs.writeFileSync(b,content,"utf8"); }
function one(src,a,b,label){ if(!src.includes(a)) fail(`No encontré bloque: ${label}`); return src.replace(a,b); }

const feed0=read(F.feed), sheet0=read(F.sheet), css0=read(F.css), notif0=read(F.notifications);
if(feed0.includes(MARKER)&&sheet0.includes(MARKER)){ console.log("✅ Comment Threads 1.0 ya estaba aplicado."); process.exit(0); }
let feed=feed0;
feed=one(feed,'"id,post_id,user_id,content,created_at"','"id,post_id,user_id,content,created_at,parent_comment_id"','select comments');
feed=one(feed,`  async function addComment(\n    postId: number,\n    rawValue: string\n  ): Promise<boolean> {`,`  async function addComment(\n    postId: number,\n    rawValue: string,\n    parentCommentId: number | null = null\n  ): Promise<boolean> {`,'firma addComment');
feed=one(feed,'    const actionKey =\n      `comment:${postId}`;','    const actionKey =\n      `comment:${postId}:${parentCommentId || "root"}`;','action key');
feed=one(feed,'      content: value,\n      profile: {','      content: value,\n      parent_comment_id:\n        parentCommentId,\n      profile: {','optimistic parent');
feed=one(feed,'          user_id: currentUser.id,\n          content: value,\n        })\n        .select("id,created_at")','          user_id: currentUser.id,\n          content: value,\n          parent_comment_id:\n            parentCommentId,\n        })\n        .select(\n          "id,created_at,parent_comment_id"\n        )','insert parent');
feed=one(feed,`    const targetPost = posts.find(\n      (item) => item.id === postId\n    );\n\n`,'','remove targetPost');
feed=one(feed,`      if (\n        targetPost &&\n        targetPost.user_id !==\n          currentUser.id\n      ) {\n        void supabase\n          .from("notifications")\n          .insert({\n            user_id:\n              targetPost.user_id,\n            actor_id:\n              currentUser.id,\n            type: "comment",\n            post_id: postId,\n            target_type:\n              "post_comment",\n            target_id:\n              String(inserted.id),\n          })\n          .then(({ error }) => {\n            if (error) {\n              console.warn(\n                "[Alumni Feed] comment notification:",\n                error\n              );\n            }\n          });\n      }\n\n`,'','remove client notification');
feed=one(feed,`          onSend={(value) =>\n            activeCommentsPost\n              ? addComment(\n                  activeCommentsPost.id,\n                  value\n                )\n              : Promise.resolve(false)\n          }`,`          onSend={(\n            value,\n            parentCommentId\n          ) =>\n            activeCommentsPost\n              ? addComment(\n                  activeCommentsPost.id,\n                  value,\n                  parentCommentId || null\n                )\n              : Promise.resolve(false)\n          }`,'FeedCommentsSheet onSend');
feed += `\n/* ${MARKER} */\n`;

const sheet="\"use client\";\n\nimport { Send, X } from \"lucide-react\";\nimport { useEffect, useMemo, useRef, useState } from \"react\";\nimport { createPortal } from \"react-dom\";\nimport CommentLikeButton from \"@/components/social/CommentLikeButton\";\n\ntype ReplyTarget = { id: number; username: string } | null;\ntype ThreadedComment = { comment: any; depth: number };\n\nfunction threadedComments(comments: any[]): ThreadedComment[] {\n  const byId = new Map<number, any>();\n  const children = new Map<number, any[]>();\n  const roots: any[] = [];\n\n  for (const comment of comments) {\n    const id = Number(comment?.id);\n    if (Number.isFinite(id) && id > 0) byId.set(id, comment);\n  }\n\n  for (const comment of comments) {\n    const parentId = Number(comment?.parent_comment_id);\n    if (Number.isFinite(parentId) && parentId > 0 && byId.has(parentId)) {\n      const list = children.get(parentId) || [];\n      list.push(comment);\n      children.set(parentId, list);\n    } else {\n      roots.push(comment);\n    }\n  }\n\n  const output: ThreadedComment[] = [];\n  const visited = new Set<number>();\n\n  function walk(comment: any, depth: number) {\n    const id = Number(comment?.id);\n    if (Number.isFinite(id) && id > 0) {\n      if (visited.has(id)) return;\n      visited.add(id);\n    }\n\n    output.push({ comment, depth });\n    for (const reply of children.get(id) || []) walk(reply, depth + 1);\n  }\n\n  for (const root of roots) walk(root, 0);\n\n  for (const comment of comments) {\n    const id = Number(comment?.id);\n    if (!Number.isFinite(id) || id <= 0 || !visited.has(id)) {\n      output.push({ comment, depth: 0 });\n    }\n  }\n\n  return output;\n}\n\nexport default function FeedCommentsSheet({\n  post,\n  currentUserId,\n  onSend,\n  onClose,\n  focusedCommentId,\n  loading = false,\n}: {\n  post: any | null;\n  currentUserId?: string | null;\n  onSend: (value: string, parentCommentId?: number | null) => Promise<boolean>;\n  onClose: () => void;\n  focusedCommentId?: number | null;\n  loading?: boolean;\n}) {\n  const [ready, setReady] = useState(false);\n  const [input, setInput] = useState(\"\");\n  const [sending, setSending] = useState(false);\n  const [replyTo, setReplyTo] = useState<ReplyTarget>(null);\n  const inputRef = useRef<HTMLInputElement>(null);\n\n  useEffect(() => setReady(true), []);\n\n  useEffect(() => {\n    setInput(\"\");\n    setSending(false);\n    setReplyTo(null);\n  }, [post?.id]);\n\n  async function submitComment() {\n    const value = input.trim();\n    if (!value || !currentUserId || sending) return;\n\n    const parentCommentId = replyTo?.id ?? null;\n    setInput(\"\");\n    setSending(true);\n\n    try {\n      const ok = await onSend(value, parentCommentId);\n      if (ok) setReplyTo(null);\n      else setInput((current) => (current.trim() ? current : value));\n    } finally {\n      setSending(false);\n    }\n  }\n\n  useEffect(() => {\n    if (!post) return;\n    const old = document.body.style.overflow;\n    document.body.style.overflow = \"hidden\";\n    return () => { document.body.style.overflow = old; };\n  }, [post]);\n\n  useEffect(() => {\n    if (!post || !focusedCommentId) return;\n    const timer = window.setTimeout(() => {\n      document.getElementById(`feed-comment-${focusedCommentId}`)?.scrollIntoView({\n        behavior: \"smooth\",\n        block: \"center\",\n      });\n    }, 180);\n    return () => window.clearTimeout(timer);\n  }, [post?.id, focusedCommentId]);\n\n  const comments = post?.comments || [];\n  const threaded = useMemo(() => threadedComments(comments), [comments]);\n\n  function beginReply(comment: any) {\n    const id = Number(comment?.id);\n    if (!currentUserId || !Number.isFinite(id) || id <= 0) return;\n    setReplyTo({ id, username: comment.profile?.username || \"usuario\" });\n    window.setTimeout(() => inputRef.current?.focus(), 20);\n  }\n\n  if (!ready || !post) return null;\n\n  return createPortal(\n    <div className=\"alumni-comments-backdrop\" onMouseDown={(event) => {\n      if (event.currentTarget === event.target) onClose();\n    }}>\n      <section className=\"alumni-comments-sheet alumni-comments-messaging-font\">\n        <header className=\"alumni-comments-header\">\n          <div><span>Publicación</span><h3>Comentarios</h3></div>\n          <button type=\"button\" onClick={onClose} aria-label=\"Cerrar comentarios\"><X size={20} /></button>\n        </header>\n\n        <div className=\"alumni-comments-list\">\n          {loading ? (\n            <div className=\"alumni-feed-modal-empty\">Cargando comentarios...</div>\n          ) : threaded.length ? (\n            threaded.map(({ comment, depth }) => {\n              const visualDepth = Math.min(depth, 3);\n              return (\n                <div\n                  id={`feed-comment-${comment.id}`}\n                  key={comment.id}\n                  className={`alumni-feed-comment alumni-thread-comment ${visualDepth > 0 ? \"is-reply\" : \"\"} ${focusedCommentId === comment.id ? \"is-focused\" : \"\"}`}\n                  data-thread-depth={visualDepth}\n                  style={{ marginLeft: visualDepth > 0 ? `${visualDepth * 18}px` : undefined }}\n                >\n                  <a href={`/u/${comment.profile?.username || \"\"}`} className=\"alumni-feed-comment-avatar\">\n                    {comment.profile?.avatar_url ? <img src={comment.profile.avatar_url} alt=\"\" /> : (comment.profile?.username?.charAt(0)?.toUpperCase() || \"A\")}\n                  </a>\n\n                  <div className=\"alumni-feed-comment-body\">\n                    <p>\n                      <a href={`/u/${comment.profile?.username || \"\"}`}>@{comment.profile?.username || \"usuario\"}</a>\n                      <span>{comment.content}</span>\n                    </p>\n\n                    <div className=\"alumni-comment-thread-actions\">\n                      {Number(comment.id) > 0 && (\n                        <CommentLikeButton\n                          commentId={comment.id}\n                          commentOwnerId={comment.user_id}\n                          currentUserId={currentUserId}\n                        />\n                      )}\n                      {currentUserId && Number(comment.id) > 0 && (\n                        <button type=\"button\" className=\"alumni-comment-reply-button\" onClick={() => beginReply(comment)}>\n                          Responder\n                        </button>\n                      )}\n                    </div>\n                  </div>\n                </div>\n              );\n            })\n          ) : (\n            <div className=\"alumni-feed-modal-empty\">Sé la primera persona en comentar.</div>\n          )}\n        </div>\n\n        <div className=\"alumni-comments-composer-wrap\">\n          {replyTo && (\n            <div className=\"alumni-comment-reply-target\">\n              <span>Respondiendo a <strong>@{replyTo.username}</strong></span>\n              <button type=\"button\" onClick={() => setReplyTo(null)} aria-label=\"Cancelar respuesta\"><X size={14} /></button>\n            </div>\n          )}\n\n          <div className=\"alumni-comments-composer\">\n            <input\n              ref={inputRef}\n              type=\"text\"\n              value={input}\n              disabled={!currentUserId}\n              placeholder={currentUserId ? (replyTo ? `Responder a @${replyTo.username}...` : \"Escribe un comentario...\") : \"Inicia sesión para comentar\"}\n              onChange={(event) => setInput(event.target.value)}\n              onKeyDown={(event) => {\n                if (event.key === \"Enter\") {\n                  event.preventDefault();\n                  void submitComment();\n                }\n              }}\n            />\n            <button\n              type=\"button\"\n              onClick={() => void submitComment()}\n              disabled={!currentUserId || !input.trim() || sending}\n              aria-busy={sending}\n              aria-label={replyTo ? \"Enviar respuesta\" : \"Enviar comentario\"}\n            >\n              <Send size={17} />\n            </button>\n          </div>\n        </div>\n      </section>\n    </div>,\n    document.body\n  );\n}\n\n/* ALUMNI_1_4_0_COMMENTS_SHEET */\n/* ALUMNI_PERFORMANCE_HARDENING_FEED_V2_LAZY_COMMENTS */\n/* ALUMNI_PERFORMANCE_HARDENING_COMMENT_DRAFT_LOCAL_V8 */\n/* ALUMNI_1_7_0_STORIES_HIDDEN_COMMENTS_GEIST */\n/* ALUMNI_COMMENT_THREADS_1_0 */\n";
let css=css0;
if(!css.includes(MARKER)) css += `\n\n/* ${MARKER} */\n.alumni-comments-messaging-font .alumni-thread-comment{position:relative;transition:background-color 160ms ease,transform 160ms ease}.alumni-comments-messaging-font .alumni-thread-comment.is-reply{width:auto}.alumni-comments-messaging-font .alumni-thread-comment.is-reply::before{content:"";position:absolute;left:-10px;top:4px;bottom:4px;width:1px;background:var(--app-border);opacity:.78}.alumni-comments-messaging-font .alumni-comment-thread-actions{display:flex;align-items:center;gap:10px;min-height:24px;margin-top:3px}.alumni-comments-messaging-font .alumni-comment-reply-button{appearance:none;border:0;background:transparent;color:var(--app-muted);padding:3px 0;font-size:12px;line-height:1.2;font-weight:650;cursor:pointer}.alumni-comments-messaging-font .alumni-comment-reply-button:hover{color:var(--app-text)}.alumni-comments-messaging-font .alumni-comments-composer-wrap{border-top:1px solid var(--app-border);background:var(--app-surface)}.alumni-comments-messaging-font .alumni-comments-composer-wrap .alumni-comments-composer{border-top:0}.alumni-comments-messaging-font .alumni-comment-reply-target{display:flex;align-items:center;justify-content:space-between;gap:12px;min-height:34px;padding:8px 14px 0;color:var(--app-muted);font-size:12px;line-height:1.25}.alumni-comments-messaging-font .alumni-comment-reply-target strong{color:var(--app-text-soft);font-weight:720}.alumni-comments-messaging-font .alumni-comment-reply-target button{display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border:0;border-radius:999px;background:var(--app-soft);color:var(--app-muted)}@media(max-width:430px){.alumni-comments-messaging-font .alumni-thread-comment[data-thread-depth="3"]{margin-left:42px!important}.alumni-comments-messaging-font .alumni-thread-comment[data-thread-depth="2"]{margin-left:30px!important}.alumni-comments-messaging-font .alumni-thread-comment[data-thread-depth="1"]{margin-left:16px!important}}\n`;

let notif=notif0;
if(!notif.includes('case "comment_reply":')){
  notif=one(notif,`    case "comment":\n      return plural\n        ? "comentaron tu publicación"\n        : "comentó tu publicación";\n\n    case "comment_like":`,`    case "comment":\n      return plural\n        ? "comentaron tu publicación"\n        : "comentó tu publicación";\n\n    case "comment_reply":\n      return plural\n        ? "respondieron a tu comentario"\n        : "respondió a tu comentario";\n\n    case "comment_like":`,'notification copy');
  notif=one(notif,`    case "comment":\n    case "story_reply":\n      return MessageCircle;`,`    case "comment":\n    case "comment_reply":\n    case "story_reply":\n      return MessageCircle;`,'notification icon');
}
notif += `\n/* ${MARKER} */\n`;
const migration="-- ALUMNI COMMENT THREADS 1.0\n-- Applied to production Supabase on 2026-09-11.\n\nalter table public.comments\n  add column if not exists parent_comment_id bigint null;\n\nalter table public.comments\n  drop constraint if exists comments_parent_comment_id_fkey;\n\nalter table public.comments\n  add constraint comments_parent_comment_id_fkey\n  foreign key (parent_comment_id)\n  references public.comments(id)\n  on delete set null;\n\ncreate index if not exists comments_parent_comment_idx\n  on public.comments(parent_comment_id, created_at, id)\n  where parent_comment_id is not null;\n\ncreate or replace function public.alumni_validate_comment_parent()\nreturns trigger\nlanguage plpgsql\nsecurity definer\nset search_path = public, pg_temp\nas $$\ndeclare\n  v_parent_post_id bigint;\nbegin\n  if new.parent_comment_id is null then return new; end if;\n\n  select c.post_id into v_parent_post_id\n  from public.comments c\n  where c.id = new.parent_comment_id;\n\n  if v_parent_post_id is null then raise exception 'COMMENT_PARENT_NOT_FOUND'; end if;\n  if v_parent_post_id is distinct from new.post_id then raise exception 'COMMENT_PARENT_POST_MISMATCH'; end if;\n  if new.id is not null and new.parent_comment_id = new.id then raise exception 'COMMENT_SELF_PARENT_NOT_ALLOWED'; end if;\n  return new;\nend;\n$$;\n\ndrop trigger if exists alumni_validate_comment_parent_trigger on public.comments;\ncreate trigger alumni_validate_comment_parent_trigger\nbefore insert or update of parent_comment_id, post_id\non public.comments\nfor each row execute function public.alumni_validate_comment_parent();\n\ncreate or replace function public.alumni_post_comments_v1(p_post_id bigint, p_limit integer default 500)\nreturns jsonb\nlanguage sql\nstable\nset search_path = ''\nas $$\n  select coalesce(\n    jsonb_agg(\n      jsonb_build_object(\n        'id', c.id,\n        'post_id', c.post_id,\n        'user_id', c.user_id,\n        'content', c.content,\n        'created_at', c.created_at,\n        'parent_comment_id', c.parent_comment_id,\n        'profile', case when p.id is null then null else jsonb_build_object(\n          'id', p.id, 'username', p.username, 'full_name', p.full_name, 'avatar_url', p.avatar_url\n        ) end\n      ) order by c.created_at asc, c.id asc\n    ),\n    '[]'::jsonb\n  )\n  from (\n    select c0.* from public.comments c0\n    where c0.post_id = p_post_id\n    order by c0.created_at desc, c0.id desc\n    limit least(greatest(coalesce(p_limit, 500), 1), 500)\n  ) c\n  left join public.profiles p on p.id = c.user_id;\n$$;\n\ncreate or replace function public.alumni_notify_comment()\nreturns trigger\nlanguage plpgsql\nsecurity definer\nset search_path = public, pg_temp\nas $$\ndeclare\n  v_owner uuid;\n  v_parent_owner uuid;\nbegin\n  select p.user_id into v_owner from public.posts p where p.id = new.post_id;\n\n  if new.parent_comment_id is not null then\n    select c.user_id into v_parent_owner from public.comments c where c.id = new.parent_comment_id;\n    if v_parent_owner is not null and v_parent_owner <> new.user_id then\n      insert into public.notifications(user_id,actor_id,type,post_id,target_type,target_id)\n      values(v_parent_owner,new.user_id,'comment_reply',new.post_id,'post_comment',new.id::text);\n    end if;\n  end if;\n\n  if v_owner is not null and v_owner <> new.user_id and v_owner is distinct from v_parent_owner then\n    insert into public.notifications(user_id,actor_id,type,post_id,target_type,target_id)\n    values(v_owner,new.user_id,'comment',new.post_id,'post_comment',new.id::text);\n  end if;\n\n  return new;\nend;\n$$;\n\nanalyze public.comments;\n";

try{ const ts=require("typescript"); for(const [rel,content] of [[F.feed,feed],[F.sheet,sheet],[F.notifications,notif]]){ const sf=ts.createSourceFile(rel,content,ts.ScriptTarget.Latest,true,ts.ScriptKind.TSX); const d=sf.parseDiagnostics||[]; if(d.length){ const x=d[0]; const msg=ts.flattenDiagnosticMessageText(x.messageText,"\n"); fail(`${rel}: ${msg}`); } } console.log("✅ Parser TypeScript: válido"); }catch(e){ if(!(e&&e.code==="MODULE_NOT_FOUND")) throw e; }
backup(F.feed,feed0); backup(F.sheet,sheet0); backup(F.css,css0); backup(F.notifications,notif0);
fs.writeFileSync(p(F.feed),feed,"utf8"); fs.writeFileSync(p(F.sheet),sheet,"utf8"); fs.writeFileSync(p(F.css),css,"utf8"); fs.writeFileSync(p(F.notifications),notif,"utf8");
fs.mkdirSync(path.dirname(p(F.migration)),{recursive:true}); if(!fs.existsSync(p(F.migration))) fs.writeFileSync(p(F.migration),migration,"utf8");
console.log("\n✅ ALUMNI Comment Threads 1.0 aplicado.\n✅ Respuestas sucesivas reales.\n✅ Notificación al autor respondido.\n✅ Diseño móvil protegido.\n\nAhora ejecutá: npm run build");
