ALUMNI VERIFIED BADGE FEED 1.0

YA APLICADO EN SUPABASE
El RPC alumni_feed_page_v2 ya devuelve:
- profiles.is_verified
- latestRepostProfile.is_verified
- latest comment profile.is_verified
- currentProfile.is_verified

PARCHE FRONTEND
Muestra BadgeCheck en:
- autor principal de cada publicación
- usuario que hizo repost
- preview del último comentario

También corrige el fallback legacy para pedir is_verified.

EJECUTAR

node .\aplicar_alumni_verified_badge_feed_1_0.js
npm run build

SI BUILD PASA

git add .
git commit -m "Show verified badges in feed"
git push origin main

RESULTADO
Verificar usuario en:
Centro de Control > Usuarios > Gestionar > Verificar

-> el check aparece en Feed.

Revocar:
Centro de Control > Usuarios > Gestionar > Revocar

-> el check desaparece.

RECORDATORIO QA
node .\scripts\qa-demo\cleanup_alumni_demo.js --confirm=DELETE-ALUMNI-QA
